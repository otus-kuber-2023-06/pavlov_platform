apiVersion: v1
kind: Pod
metadata:
  labels:
    run: frontend
  name: frontend
spec:
  containers:
  - image: minized/hipster-frontend:v0.0.1
    name: frontend
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
